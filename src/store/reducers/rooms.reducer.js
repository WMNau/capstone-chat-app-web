import {} from "../actions/types";

const initialState = {
  messageError: null,
};

const roomsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default roomsReducer;
